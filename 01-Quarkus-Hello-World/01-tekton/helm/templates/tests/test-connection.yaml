apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quarkus-jvm-helloworld.fullname" . }}-test-connection"
  labels:
    {{- include "quarkus-jvm-helloworld.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quarkus-jvm-helloworld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
