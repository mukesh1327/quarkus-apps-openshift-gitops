apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  annotations:
    chains.tekton.dev/signed: 'true'

  name: quarkus-jvm-helloworld-helm-pr

  labels:
    tekton.dev/pipeline: quarkus-jvm-helloworld-helm

spec:

  pipelineRef:
    name: quarkus-jvm-helloworld-helm


  taskRunTemplate:
    serviceAccountName: pipeline

  timeouts:
    pipeline: 1h0m0s

  workspaces:
    - name: source-code
      persistentVolumeClaim:
        claimName: source-code-workspace
    - name: maven-packages
      persistentVolumeClaim:
        claimName: maven-workspace
    - name: manifests
      persistentVolumeClaim:
        claimName: manifests-workspace
        
